# 本次修改简述（2026-09-12）

> 范围：zhibi-vantage 工作台首页概览重构、面板内容区内边距统一、侧边栏一级菜单选中态与品牌色调整、设计规范合并、后端启动排查与 LLM 配置。

## 一、前端 UI / 样式

### 1. 首页概览三卡布局重构（`overview-cards.tsx` + `globals.css`）
- 赛道卡（指定赛道）：占满整行（`grid-column:span 3`），输入框宽度保持不变（`width:66.6%`，不随卡片拉伸）
- 品牌检索卡 + 雷达卡：改为同一行；两者高度均 `240px`；DOM 顺序调换后**品牌在左、雷达在右**
- 窄屏（≤880px）恢复单列、高度 auto、输入框占满

### 2. 面板内容区内边距统一（`globals.css` + `(panel)/layout.tsx`）
- 统一内边距上提到布局层 `<main>`：`padding:46px 54px 60px`（桌面），`24px 16px 48px`（移动端 ≤640px）
- 修复首页贴边问题：移除 `.wb-page` 多余 padding（之前与 `.content` 叠加导致首页实际无内边距；`.content` 不在面板布局 DOM 中）
- 一级菜单页面（情报库 / 机会地图 / 竞品雷达 / 赛道档案 / 历史调研 / 算法资产 / 设置 / 调研报告）内容区现与首页一致

### 3. 侧边栏一级菜单选中态（`.nav-item.on/.active`）
演变路径：绿色渐变 → 纯色 `#8EF69A` → 半透明 `rgba(142,246,154,.45)` → **浅灰白实底 `#F0F0EE`（无透明度）** → **去除阴影**
最终态：`background:#F0F0EE; color:#04160C; font-weight:700`（无 box-shadow）
与顶部 Pill Nav 的深色选中态（`#0D0D0D`/`#fff`）区分，两者为不同组件。

### 4. 品牌高亮色统一（`globals.css`）
- `--brand-hi` 由 `#3DD68C` 改为 `#8EF69A`，作为全站绿色高亮文本 / 强调色 / 状态点的统一 token（一处改动全站生效）
- `--brand` 主绿 `#6EE7A0`、英雄渐变 `--gradient-hero`（三色薄荷绿）保持不变

## 二、设计规范

- `docs/design.md` 合并了原 `docs/设计规范.md`：纳入完整设计令牌（基础色板 / 边框 / 圆角 / 间距 / 字体 / 阴影 / 组件 / 布局），强调色统一以项目变量 `--brand` / `--brand-hi` 为准（不改代码）
- 文档注明首页概览态（无赛道）卡片为纯白 / 深色填充、未使用绿色渐变，`--gradient-hero` 作为备用 token 保留
- 注：`docs/设计规范.md` 现仍冗余存在，待清理（未纳入本次提交）

## 三、后端 / 配置

- LLM 已配置（`app/data/config.json` → `llm`）：阿里云 MaaS OpenAI 兼容端点 + 模型 `deepseek-v4-flash`，已单独测试可用
- **搜索源 key 缺失**：`search` 配置所有 key 为空（provider=serper 但无 serperKey）。竞品识别 `/api/discover` 必须先有搜索结果，因此完整竞品调研目前无法跑通，需补充 serper / tavily / brave / bocha 任一 key
- 修复 `/api/discover` 报错 `HTTP 500`：根因为 **zhibi 后端 `app/server.js` 未运行**（`/api` 代理目标 `127.0.0.1:3300` 无服务 → 空体 500），与原模型配置无关。已启动后端于 `:3300` 验证 `/healthz` 返回 200，discover 现返回带体的正常业务响应

## 四、待办（未包含在本次提交）

1. 补充搜索源 API key，使竞品识别完整链路可用
2. 清理冗余的 `docs/设计规范.md`
3. 竞品识别端到端测试（需搜索 key 就绪后）

## 五、提交文件清单

- `web/src/app/(panel)/layout.tsx`
- `web/src/app/(panel)/page.tsx`
- `web/src/app/globals.css`
- `web/src/components/onboarding.tsx`
- `web/src/components/overview-cards.tsx`（新增）
- `docs/design.md`（新增，合并版）
- `docs/2026-09-12-改动简述.md`（本文档）

> 未纳入：`.gitignore` 已忽略 `app/data/config.json`（含明文密钥）；`docs/设计规范.md` 冗余未提交；`web/package-lock.json` 未纳入，按需另行提交。
